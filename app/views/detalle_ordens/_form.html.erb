<%= form_with(model: detalle_orden, local: true) do |form| %>
  <% if detalle_orden.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(detalle_orden.errors.count, "error") %> prohibited this detalle_orden from being saved:</h2>

      <ul>
      <% detalle_orden.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cantidad :id=>cantlabel%>
    <%= form.number_field :cantidad :id=>cantnum%>
  </div>

  <div class="field">
    <%= form.label :precio_venta id=>preciolab %>
    <%= form.text_field :precio_venta id=>preciotext%>
  </div>

  <div class ="field">
    <%= form.select :producto_id, Producto.all.collect {|x|[x.nombre_producto, x.id]}, {:include_blank => "Selecciona un producto"} id=>seleccion%>
  </div>

  <div class="actions">
    <%= form.submit id=>enviar%>
  </div>
<% end %>
